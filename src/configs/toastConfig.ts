import { ToastContainerProps, toast } from "react-toastify";

// Exporta a configuração padrão para o container de toast
export default {
  // Define a posição das notificações como no topo central da tela
  position: toast.POSITION.TOP_CENTER,
} as ToastContainerProps;
